{
  "metadata": {
    "name": "Context Overlay App Example"
  },
  "$": {
    "controller": "${REPL_ACCOUNT}/widget/ContextOverlayExample.Main",
    "targets": [
      {
        "namespace": "${REPL_ACCOUNT}/parser/common-body-test",
        "contextType": "body",
        "if": { "id": { "not": null, "index": true } }
      },
      {
        "namespace": "${REPL_ACCOUNT}/parser/common-body",
        "contextType": "body",
        "if": { "id": { "not": null, "index": true } }
      }
    ]
  }
}
